import Header from "../components/Header";

function Home(props) {
    return (
        <div>
            <Header />
            <main className="text-center">
                <h2 className="pageTitle">Home</h2>
            </main>
        </div>
    );
}

export default Home;
